package com.example.walkitoff;

public class Observer {

    /**
     * observer of the distance class, which will then notify the score class
     * of any changes
     */

    public Observer(){

    }

    /**
     * distance class will call this method to notify of the distance travelled
     * in order to update the user score
     * 
     * @param distance
     */
    public void updateScore( int distance ){

        // instantiate score object

        // call update the score object with the distance
    }
}
